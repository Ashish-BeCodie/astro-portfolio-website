---
import NoData from "../../components/NoData.astro";
import PageTitle from "../../components/PageTitle.astro";
import Layout from "../../layouts/Layout.astro";
---

<Layout title="BeCodie — Projects">
  <PageTitle
    title="Below are some of the works I've involved with"
    subtitle="Projects"
  />

  <!--  <article>
    {
      Projects.length ? (
        <div class="lg:-ml-10 lg:-mr-10 grid lg:grid-cols-2 gap-8 gap-y-16">
          {Projects.map((project, index) => (
            <ProjectCard project={project} index={index+1}/>
          ))}
        </div>
      ) : (
        <p>No projects added yet!</p>
      )
    }
  </article> -->

  <NoData text="No projects added yet!" />
</Layout>
